---
phase: 03-voice-mvp-tier-1
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - apps/backend/src/routes/menu-items.ts
  - apps/backend/src/routes/menu-categories.ts
  - apps/backend/src/routes/onboarding.ts
  - apps/backend/src/__tests__/menu-items.test.ts
autonomous: false
requirements: [ONB-05, MENU-05]

must_haves:
  truths:
    - "Finishing onboarding creates a Vapi assistant and stores its id on the restaurant (ONB-05)"
    - "Editing any menu item or category triggers a fire-and-forget Vapi prompt resync (MENU-05)"
    - "A menu edit still succeeds (HTTP 2xx) even if the Vapi sync fails"
  artifacts:
    - path: "apps/backend/src/routes/onboarding.ts"
      provides: "createVapiAssistant call after Twilio provision, persists vapi_assistant_id"
      contains: "createVapiAssistant"
    - path: "apps/backend/src/routes/menu-items.ts"
      provides: "syncAssistantPrompt fire-and-forget on POST/PATCH/DELETE/availability"
      contains: "syncAssistantPrompt"
    - path: "apps/backend/src/routes/menu-categories.ts"
      provides: "syncAssistantPrompt fire-and-forget on POST/PATCH/DELETE"
      contains: "syncAssistantPrompt"
  key_links:
    - from: "apps/backend/src/routes/onboarding.ts"
      to: "lib/vapi.createVapiAssistant"
      via: "import + call after Twilio provision"
      pattern: "createVapiAssistant"
    - from: "apps/backend/src/routes/menu-items.ts"
      to: "lib/vapi.syncAssistantPrompt"
      via: "fire-and-forget .catch()"
      pattern: "syncAssistantPrompt\\(req.restaurantId\\)"
---

<objective>
Wire the lib/vapi exports into the existing flows: onboarding creates the assistant (ONB-05), and every menu mutation resyncs the assistant prompt within seconds (MENU-05). All additive — no existing handler logic changes.

Purpose: Without this wiring, the assistant is never created and never reflects menu changes. These are the connective tissue that makes the agent read the real, current menu.
Output: onboarding.ts + menu-items.ts + menu-categories.ts modified; a menu-items.test.ts case asserts the sync call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-voice-mvp-tier-1/03-CONTEXT.md
@.planning/phases/03-voice-mvp-tier-1/03-PATTERNS.md

<interfaces>
<!-- Source: verified codebase + Plan 02 -->

lib/vapi.ts (Plan 02):
  createVapiAssistant(restaurant: { id, name, agent_name }): Promise<string>  // returns assistantId
  syncAssistantPrompt(restaurantId: string): Promise<void>  // swallows Vapi errors internally

menu-items.ts (AuthedRequest with req.restaurantId) success-path return lines:
  POST /        → return res.status(201).json({ item: finalItem });   (~line 188)
  PATCH /:id    → return res.json({ item: updatedItem });             (~line 315)
  DELETE /:id   → return res.status(204).send();                      (~line 334)
  PATCH /:id/availability → return res.json(data);                    (~line 355)

menu-categories.ts (AuthedRequest with req.restaurantId) success-path return lines:
  POST /        → return res.status(201).json({ category });          (~line 70)
  PATCH /:id    → return res.json({ category });                      (~line 102)
  DELETE /:id   → return res.status(204).send();                      (~line 122)

onboarding.ts POST /finish: after the restaurant UPDATE (twilio_number, onboarding_step:4) succeeds
  at ~line 104, before the final return res.json({...}). `restaurant` has { id, onboarding_step, twilio_number };
  it does NOT select name/agent_name yet — extend the select or re-fetch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: onboarding.ts — create Vapi assistant on finish (ONB-05)</name>
  <read_first>
    - apps/backend/src/routes/onboarding.ts (POST /finish handler, the restaurant select + update, the final return)
    - apps/backend/src/lib/vapi.ts (createVapiAssistant signature)
    - .planning/phases/03-voice-mvp-tier-1/03-PATTERNS.md lines 236-268 (exact placement + log-but-don't-fail pattern)
  </read_first>
  <action>
    Modify `apps/backend/src/routes/onboarding.ts`:
    1. Add import: `import { createVapiAssistant } from '../lib/vapi';`
    2. The `/finish` handler selects `id, onboarding_step, twilio_number`. Extend that select to also fetch `name, agent_name`: change to `.select('id, onboarding_step, twilio_number, name, agent_name')`.
    3. After the successful restaurant UPDATE (the one that sets twilio_number + onboarding_step:4) and BEFORE the final `return res.json({...})`, insert a non-blocking assistant-creation block (copy PATTERNS lines 243-260 verbatim):
       ```typescript
       // Create Vapi assistant (ONB-05). Non-blocking: Twilio provision is the critical path;
       // an assistant-creation failure must NOT roll back onboarding (avoid re-provisioning a number).
       try {
         const assistantId = await createVapiAssistant({
           id: restaurant.id,
           name: restaurant.name,
           agent_name: restaurant.agent_name ?? 'Sofía',
         });
         await supabaseAdmin
           .from('restaurants')
           .update({ vapi_assistant_id: assistantId })
           .eq('id', restaurant.id)
           .eq('owner_id', authedReq.userId); // defense-in-depth
       } catch (vapiErr) {
         logger.error('vapi assistant creation failed', {
           restaurant_id: restaurant.id,
           error: String(vapiErr),
         });
       }
       ```
    4. Also handle the idempotent early-return branch (when onboarding already finished with a twilio_number): if `restaurant.vapi_assistant_id` is null there, the same createVapiAssistant block can run so a retry creates a missing assistant. (Add `vapi_assistant_id` to the select and guard the create with `if (!restaurant.vapi_assistant_id)` in the early-return branch.)
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente restaurante" && grep -q "createVapiAssistant" apps/backend/src/routes/onboarding.ts && grep -q "vapi_assistant_id" apps/backend/src/routes/onboarding.ts && pnpm --filter backend run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - onboarding.ts imports `createVapiAssistant` from `'../lib/vapi'`
    - The /finish select includes `name, agent_name, vapi_assistant_id`
    - onboarding.ts contains a try/catch calling createVapiAssistant then updating `vapi_assistant_id`, with `logger.error` on failure (no rethrow)
    - `pnpm --filter backend run lint` exits 0
  </acceptance_criteria>
  <done>Onboarding finish creates the Vapi assistant and persists its id; failure logs without breaking onboarding.</done>
</task>

<task type="auto">
  <name>Task 2: menu-items.ts + menu-categories.ts — fire-and-forget resync (MENU-05)</name>
  <read_first>
    - apps/backend/src/routes/menu-items.ts (the four success-path returns at ~188, ~315, ~334, ~355; current imports)
    - apps/backend/src/routes/menu-categories.ts (the three success-path returns at ~70, ~102, ~122; current imports)
    - apps/backend/src/lib/vapi.ts (syncAssistantPrompt signature)
    - apps/backend/src/__tests__/menu-items.test.ts (where to add the sync assertion)
    - .planning/phases/03-voice-mvp-tier-1/03-PATTERNS.md lines 193-232 (exact placement + fire-and-forget shape)
  </read_first>
  <action>
    Both files: add imports at top (check if logger already imported first):
      `import { syncAssistantPrompt } from '../lib/vapi';`
      `import { logger } from '../lib/logger';`  (only if not already present)

    Insert this fire-and-forget call IMMEDIATELY BEFORE each listed success-path `return` (NOT after; the statement must execute before returning the response object — capture restaurantId first):
      ```typescript
      // MENU-05: resync the Vapi system prompt with the new menu. Fire-and-forget —
      // the menu edit must succeed even if Vapi is down. syncAssistantPrompt swallows its own errors;
      // the .catch is belt-and-suspenders. Do NOT await.
      syncAssistantPrompt(req.restaurantId).catch((err) => {
        logger.error('vapi sync failed after menu edit', { error: String(err), restaurant_id: req.restaurantId });
      });
      ```

    menu-items.ts — add before the success returns in: POST / , PATCH /:id , DELETE /:id , PATCH /:id/availability.
    menu-categories.ts — add before the success returns in: POST / , PATCH /:id , DELETE /:id.

    Do NOT `await` the call. Do NOT change any existing handler logic, status codes, or response bodies.

    Then add a unit test to `apps/backend/src/__tests__/menu-items.test.ts` that asserts MENU-05 wiring: using `vi.mock('../lib/vapi')` to spy on `syncAssistantPrompt`, assert it is called after a successful availability toggle (PATCH /:id/availability). Keep it in the "Mocked unit tests" describe block; mock supabaseAdmin as the existing tests do.
  </action>
  <verify>
    <automated>cd "/Users/mauriciocarenanew/Desktop/Repositorios/Agente restaurante" && grep -c "syncAssistantPrompt(req.restaurantId)" apps/backend/src/routes/menu-items.ts && grep -c "syncAssistantPrompt(req.restaurantId)" apps/backend/src/routes/menu-categories.ts && pnpm --filter backend run test menu-items 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - menu-items.ts contains `syncAssistantPrompt(req.restaurantId)` exactly 4 times (POST, PATCH, DELETE, availability)
    - menu-categories.ts contains `syncAssistantPrompt(req.restaurantId)` exactly 3 times (POST, PATCH, DELETE)
    - No `await syncAssistantPrompt` anywhere (grep `await syncAssistantPrompt` returns nothing)
    - menu-items.test.ts contains a `vi.mock('../lib/vapi')` and an assertion that syncAssistantPrompt was called
    - `pnpm --filter backend run test menu-items` exits 0
  </acceptance_criteria>
  <done>Every menu mutation triggers a non-awaited prompt resync; a test proves the wiring; menu edits unaffected on Vapi failure.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Seed "wonder" dev hours + Railway env vars (D-02, D-03)</name>
  <action>
    Two manual setup items Claude cannot fully self-serve. Present them clearly and wait.

    A) Railway env vars (needed before any web call reaches the deployed backend — D-03):
       The webhook on Railway needs `VAPI_API_KEY` and `VAPI_WEBHOOK_SECRET` (currently only in local .env).
       - Claude attempts via Railway CLI first: `railway variables set VAPI_API_KEY=... VAPI_WEBHOOK_SECRET=...` (read the values from apps/backend/.env). If the CLI is authenticated and works, this is automated — only fall back to manual if the CLI errors.
       - Manual fallback: user opens Railway dashboard → backend service → Variables → adds VAPI_API_KEY and VAPI_WEBHOOK_SECRET → redeploy.

    B) Dev business hours for "wonder" (D-02 — open all day so web-call tests aren't blocked by CALL-07):
       - Claude applies via Supabase MCP `execute_sql`: for restaurant_id `64e86521-7aaa-426a-82f4-d26f82680d63`, upsert restaurant_hours for day_of_week 0..6 with open_time '00:00', close_time '23:59', is_closed false. (One row per day; ON CONFLICT update.)
       - If the MCP cannot run it, fall back to SQL Editor with the same statements.
  </action>
  <verify>
    <automated>SKIPPED — Railway env + live DB state verified via external tooling, not the local shell. Confirm via Railway CLI `railway variables` (VAPI_API_KEY + VAPI_WEBHOOK_SECRET present) and Supabase MCP `SELECT day_of_week, is_closed FROM restaurant_hours WHERE restaurant_id='64e86521-7aaa-426a-82f4-d26f82680d63'` (7 rows, is_closed=false). Record both in SUMMARY per the acceptance criteria.</automated>
  </verify>
  <acceptance_criteria>
    - Railway has VAPI_API_KEY and VAPI_WEBHOOK_SECRET set (CLI `railway variables` output or user confirmation recorded in SUMMARY)
    - restaurant_hours for wonder has 7 rows, all is_closed=false, 00:00–23:59 (Supabase MCP query output recorded in SUMMARY)
  </acceptance_criteria>
  <resume-signal>Type "done" once Railway vars are set and the wonder hours query returns 7 open rows.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner menu edit → Vapi sync | Authenticated owner action triggers an outbound assistant update |
| Onboarding finish → Vapi create | Authenticated owner action provisions an external assistant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Denial of Service | Vapi outage blocks onboarding/menu edits | mitigate | Both flows wrap Vapi calls in try/catch; onboarding logs and continues, menu sync is fire-and-forget non-awaited. |
| T-03-13 | Tampering | Cross-tenant assistant update | mitigate | syncAssistantPrompt is called with req.restaurantId (JWT-derived); onboarding update scoped by owner_id + id (defense-in-depth). |
| T-03-14 | Information Disclosure | VAPI secrets in client/logs | accept | Secrets live only in Railway env + local .env; never sent to frontend; not logged. |
</threat_model>

<verification>
- `pnpm --filter backend run test` full backend suite GREEN (menu-items sync test included)
- onboarding.ts + both menu routers contain the sync/create calls
- Railway VAPI_* set; wonder hours open 7 days (recorded in SUMMARY)
</verification>

<success_criteria>
- Onboarding finish creates + persists the Vapi assistant id (ONB-05)
- All 7 menu mutation paths fire syncAssistantPrompt non-blocking (MENU-05)
- Menu edits succeed even if Vapi is down; test proves the wiring
- Railway env + dev hours seeded for web-call testing
</success_criteria>

<output>
After completion, create `.planning/phases/03-voice-mvp-tier-1/03-04-SUMMARY.md`
</output>
</content>
