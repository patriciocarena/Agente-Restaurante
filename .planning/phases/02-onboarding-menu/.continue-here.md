# Continue Here — Phase 02: Onboarding & Menu (PAUSED)

**Paused:** 2026-06-09
**Position:** Plan 02-06, Task 2 de 2 (checkpoint UAT manual) — UAT INCOMPLETA
**Reason:** Usuario decidió priorizar Phase 3 (Voice MVP / agente de voz) — el core value del producto. La UI de onboarding se termina después.

## Estado al pausar

- Los 6 planes de la fase están implementados y commiteados (último: `895b5dd` + fixes `cd41915`).
- UAT (17 pasos) quedó a mitad: el Paso 1 del wizard pasó una vez (creó el restaurante "wonder"), pero al reintentar la prueba un mes después, el wizard tira `slug_taken` (409). Pasos 2-4, MenuEditor, Settings y realtime: **sin probar**.

## Blocker conocido (resolver al retomar)

**`slug_taken` en Paso 1 del wizard.** El restaurante "wonder" ya existe en DB (owner `5f5fe6b8-1194-4a30-84b8-11d1c9520e88`, `onboarding_step=0`), pero el flujo de resume no lo detecta y el frontend re-intenta `POST /api/restaurants`.

Fix probable (elegir al retomar):
1. `useOnboardingResume` debería usar `GET /api/onboarding/resume` (`has_restaurant: true`) para saltar al paso correcto en vez de mostrar el Paso 1 virgen. Revisar por qué `onboarding_step` quedó en 0 — el frontend nunca hizo el PATCH que lo avanza (la sesión murió en el viejo bug de auth, ya arreglado en `cd41915`).
2. Y/o que el Paso 1 sea idempotente: si el slug pertenece al propio usuario, hacer PATCH en vez de fallar.
3. Workaround rápido para re-testear de cero: borrar la fila de `restaurants` (cascade limpia hours/counters/subscriptions) en Supabase.

## Deuda técnica (quitar antes de cerrar la fase)

Mensajes de diagnóstico **temporales** agregados durante el debugging:
- `apps/backend/src/routes/restaurants.ts` — el 400 de POST devuelve `details` con el error crudo de Postgres (info leak, no dejar en producción) + `console.error('[DIAG] ...')`.
- `apps/frontend/src/hooks/useRestaurantSetup.ts` — error verboso con JSON.stringify de details + `console.error('[DIAG] ...')`.

## Config local (no obvia)

- Backend dev en puerto **8787** (3000/3001 ocupados por otras apps Next.js del usuario). `apps/backend/.env` → `PORT=8787`; `apps/frontend/.env.local` → `VITE_API_URL=http://localhost:8787`.
- `dev` script del backend usa `tsx watch --env-file .env` (tsx no carga .env solo).
- CORS habilitado en `apps/backend/src/index.ts` para localhost:5173/4173 + `FRONTEND_URL`.
- Supabase free tier **se pausa tras ~1 semana sin uso** → restaurar vía Dashboard o Management API (MCP `supabase` configurado, scope user).

## Cómo retomar

1. `/gsd-resume-work` o leer este archivo.
2. Levantar backend (`apps/backend && pnpm dev`, puerto 8787) y frontend (`apps/frontend && pnpm dev`).
3. Resolver el blocker `slug_taken` (arriba).
4. Completar la UAT de 02-06-PLAN.md Task 2 (17 pasos).
5. Quitar mensajes de diagnóstico.
6. Actualizar 02-06-SUMMARY.md con resultado UAT, cerrar fase en STATE.md y ROADMAP.md.
