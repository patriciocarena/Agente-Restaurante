---
phase: quick-260612-m94
plan: 01
subsystem: backend/tooling
tags: [yaml, cli, restaurant-seed, operator-tooling, tdd]
dependency_graph:
  requires: [supabase-schema, vapi-lib, whatsapp-lib]
  provides: [restaurant-yaml-parser, cargar-restaurante-cli, wonder-pilot-data]
  affects: [apps/backend/src/lib, apps/backend/src/scripts, restaurantes/]
tech_stack:
  added: [yaml ^2.7.1]
  patterns: [pure-lib + CLI separation, TDD red/green, operator YAML config]
key_files:
  created:
    - apps/backend/src/lib/restaurant-yaml.ts
    - apps/backend/src/scripts/cargar-restaurante.ts
    - apps/backend/src/__tests__/restaurant-yaml.test.ts
    - restaurantes/wonder.yaml
    - restaurantes/COMO-CARGAR.md
  modified:
    - apps/backend/package.json
    - package.json
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
decisions:
  - "Carga manual de restaurantes por YAML reemplaza el onboarding web; frontend congelado como legacy (2026-06-12)"
  - "24:00 normalizado a 23:59:59 para compatibilidad con tipo time de Postgres"
  - "Rangos de horario que cruzan medianoche (ej: 20:00-01:00) almacenados tal cual sin validar close>open"
  - "Items ausentes del YAML marcados available=false (no delete — preserva FKs de order_items)"
metrics:
  duration: ~35min
  completed: 2026-06-12
  tasks: 3
  files: 10
---

# Quick Task 260612-m94: Carga Manual de Restaurantes YAML Script

**One-liner:** CLI operator tool (`pnpm cargar-restaurante`) that reads a Spanish YAML file, validates all fields at once, upserts restaurant + hours + menu to Supabase via service role, syncs the Vapi assistant, and supports `--dry-run`; closes Phase 2 web-UI onboarding as superseded.

## What Was Built

### Task 1 — Módulo puro restaurant-yaml.ts (TDD)

`apps/backend/src/lib/restaurant-yaml.ts` exports two pure functions (no DB, no Vapi):

- **`parseHorarios(raw)`** — maps Spanish day names/ranges to 7 `HoursRow` objects. Handles `todos`, `lun-jue`, day names with/without accents (mié/mie, sáb/sab), `cerrado`, 24:00→23:59:59 normalization, and midnight-crossing ranges stored as-is.
- **`parseRestaurantYaml(yamlString)`** — validates the entire file and collects ALL errors into a Spanish array before returning. Validates nombre, direccion, whatsapp (via `normalizeArWhatsApp`), horarios, and menu (categories + items with integer price ≥ 0).

10 unit tests (vitest), all passing. Added `yaml ^2.7.1` dependency.

### Task 2 — CLI cargar-restaurante.ts

`apps/backend/src/scripts/cargar-restaurante.ts` — full operator CLI:

1. Reads YAML file path from argv; error with usage example if missing
2. Validates with `parseRestaurantYaml`; prints all errors numbered in Spanish and exits 1
3. Looks up restaurant by slug via `supabaseAdmin`
4. UPDATE mode if exists; CREATE mode if new (requires `owner_email` field in YAML to resolve `owner_id`)
5. Replaces all 7 `restaurant_hours` rows
6. Upserts menu categories and items by name; items absent from YAML marked `available=false` (preserves FK integrity)
7. Creates or syncs Vapi assistant
8. Prints Spanish summary: created/updated/unchanged counts + "✓ Agente resincronizado"

`--dry-run` runs steps 1-3 only, prints full change preview, exits 0 without writing anything.

Scripts added: `apps/backend/package.json` → `"cargar-restaurante": "tsx --env-file .env ..."` and root `package.json` → `"cargar-restaurante": "pnpm --filter @agente-restaurante/backend cargar-restaurante --"` (the `--` passes subsequent args through to tsx).

### Task 3 — wonder.yaml + COMO-CARGAR.md + planning docs

- `restaurantes/wonder.yaml`: pilot data — Alex agent, 4 categories, 12 items, horarios todos 00:00-23:59 (testing), zonas_delivery Allende area
- `restaurantes/COMO-CARGAR.md`: plain-Spanish guide for non-technical operator covering edit patterns, the command, `--dry-run`, error handling, and security note about service role key
- `ROADMAP.md`: Phase 2 marked superseded by YAML manual loading
- `REQUIREMENTS.md`: ONB-01..04,06 and MENU-01,02,04 marked superseded; ONB-05/MENU-05 covered by cargar-restaurante
- `PROJECT.md`: key decision row added (2026-06-12, QUICK-M94)

## Commits

| Hash | Type | Description |
|------|------|-------------|
| b3b071a | test | RED: failing tests for parseHorarios and parseRestaurantYaml |
| b514caf | feat | GREEN: implement restaurant-yaml pure parsing module |
| d549a31 | feat | CLI cargar-restaurante — upsert Supabase + sync Vapi + --dry-run |
| 8336990 | feat | wonder.yaml + COMO-CARGAR.md + close phases 1-2 web-UI as superseded |

## Verification

- `pnpm test` (backend): 12 passed, 3 skipped — all green including 10 new restaurant-yaml tests
- `pnpm tsc --noEmit`: clean
- Task 3 shell check: `test -f restaurantes/wonder.yaml && grep -q "superseded" .planning/ROADMAP.md && grep -qi "carga manual" .planning/PROJECT.md` → OK

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] TypeScript strict null on categoryId after process.exit**
- **Found during:** Task 2 tsc check
- **Issue:** `categoryId` typed `string | undefined` — TS doesn't narrow through `process.exit(1)`
- **Fix:** Added `as string` cast after the guarded assignment
- **Files modified:** `apps/backend/src/scripts/cargar-restaurante.ts`
- **Commit:** d549a31

## Known Stubs

None — wonder.yaml contains real pilot data. The script does not run against the live DB until the operator runs it with a real `.env`; that is intentional (constraint from plan: no live DB run during CI).

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: credential-scope | apps/backend/src/scripts/cargar-restaurante.ts | Uses supabaseAdmin (service role key, bypasses RLS). Mitigated by design: script is operator-local-only, never deployed to Railway, `.env` is gitignored. Documented in COMO-CARGAR.md security note. |

## Self-Check: PASSED

- `restaurantes/wonder.yaml` exists: FOUND
- `restaurantes/COMO-CARGAR.md` exists: FOUND
- `apps/backend/src/lib/restaurant-yaml.ts` exists: FOUND
- `apps/backend/src/scripts/cargar-restaurante.ts` exists: FOUND
- `apps/backend/src/__tests__/restaurant-yaml.test.ts` exists: FOUND
- Commit b3b071a: FOUND
- Commit b514caf: FOUND
- Commit d549a31: FOUND
- Commit 8336990: FOUND
